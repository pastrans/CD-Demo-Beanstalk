version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - printenv
      - npm run start

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: ./